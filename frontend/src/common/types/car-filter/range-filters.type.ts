type RangeFiltersType = {
  year: {
    yearStart: string;
    yearEnd: string;
  };
  price: {
    priceStart: string;
    priceEnd: string;
  };
  race: {
    raceStart: string;
    raceEnd: string;
  };
};

export type { RangeFiltersType };
