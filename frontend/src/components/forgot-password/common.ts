import { ForgotPasswordRequestData } from '@autoline/shared/common/types/types';

const DEFAULT_FORGOT_PASSWORD_PAYLOAD: ForgotPasswordRequestData = {
  email: '',
};

export { DEFAULT_FORGOT_PASSWORD_PAYLOAD };
