import { ResetPasswordRequestData } from '@autoline/shared/common/types/types';

const DEFAULT_RESET_PASSWORD_PAYLOAD: ResetPasswordRequestData = {
  password: '',
  confirmPwd: '',
};

export { DEFAULT_RESET_PASSWORD_PAYLOAD };
